# Extra runtime tools a campaign needs beyond the babs + mechababs packages.
# Installed by bootstrap.sh into the campaign venv. Pin as needed.
con-duct
visidata  # interactive TSV viewer for DATASETS_STATE.tsv
